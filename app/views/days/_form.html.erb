<div class="form-row form-group-lg">
  <div class="col">
  <%= f.text_field :title, placeholder: "Title ie. Day One", class: "form-control"%><br>
  </div>
</div>
<div class="form-row form-group-lg">
  <div class="col">
  <%= f.date_field :date, class: "form-control" %>
  </div>
</div>
<br>
<div class="actions">
  <%= f.submit "Add New Day", class: 'btn btn-sm btn-primary' %>
</div>

<br>
<% if !@errors.nil? %>
  <div class="error">
      <label><%= pluralize(@errors.count, "error") %> prohibited this pin from
      being saved:</label>
    <ul>
      <% @errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>  
  </div>  
<% end %>