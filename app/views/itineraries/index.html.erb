<h2>Latest Updates</h2>
<div class="latest-updates"></div>
<ul>
  <% @itineraries.reverse.each do |i| %>
  <li><%= link_to "#{i.trip_title} by: #{i.users[0].firstname} on #{i.created_at.strftime("%m/%d/%Y")}", itinerary_path(i)%></li>
  <% end %>
</ul>